namespace SortPuzzle.Data
{
    /// <summary>
    /// Identifies which boost type is being used, purchased, or granted.
    /// </summary>
    public enum BoostType
    {
        Undo,
        ExtraEmptyTube
    }
}
